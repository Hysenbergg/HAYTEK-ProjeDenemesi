import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        paddingLeft: 5,
        margin: 10,
        borderRadius: 8,
        flexDirection: "row",
    },
    input: {
        flex: 1 
    }

})