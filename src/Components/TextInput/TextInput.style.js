import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 10,
        margin: 10,
        backgroundColor: 'white',
        borderRadius: 20,
        flexDirection: 'row',
        borderWidth: 1,
        alignItems: 'center',
        borderColor: 'purple'
    },
    input: {
        flex: 1,
    }
})