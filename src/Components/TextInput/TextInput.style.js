import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 3,
        marginLeft: 35,
        marginRight: 35,
        marginTop: 10,
        marginBottom: 10,
        borderColor: 'purple',
        borderWidth: 1,
        borderRadius: 10,
        justifyContent: 'space-between',
        flexDirection: 'row',
        alignItems: 'center',
    },
})